/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  WorkflowColumn,
  WorkflowColumn$inboundSchema,
} from "./workflow-column.js";
import {
  WorkflowListItem,
  WorkflowListItem$inboundSchema,
} from "./workflow-list-item.js";

export type WorkflowListData = {
  items?: Array<WorkflowListItem> | undefined;
  total: number;
  page: number;
  pageSize: number;
  hasNextPage: boolean;
  permission: string;
  columns?: Array<WorkflowColumn> | undefined;
  classifications?: { [k: string]: string } | undefined;
  message: string;
};

/** @internal */
export const WorkflowListData$inboundSchema: z.ZodMiniType<
  WorkflowListData,
  unknown
> = z.pipe(
  z.object({
    items: types.optional(z.array(WorkflowListItem$inboundSchema)),
    total: types.number(),
    page: types.number(),
    page_size: types.number(),
    has_next_page: types.boolean(),
    permission: z._default(types.string(), "read"),
    columns: types.optional(z.array(WorkflowColumn$inboundSchema)),
    classifications: types.optional(z.record(z.string(), types.string())),
    message: z._default(types.string(), "OK"),
  }),
  z.transform((v) => {
    return remap$(v, {
      "page_size": "pageSize",
      "has_next_page": "hasNextPage",
    });
  }),
);

export function workflowListDataFromJSON(
  jsonString: string,
): SafeParseResult<WorkflowListData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkflowListData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkflowListData' from JSON`,
  );
}
