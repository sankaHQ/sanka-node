/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type PositionJobData = {
  id: string;
  displayId: number;
  name: string;
  status: string;
  applicantCount: number;
  interviewCount: number;
};

/** @internal */
export const PositionJobData$inboundSchema: z.ZodMiniType<
  PositionJobData,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    display_id: types.number(),
    name: types.string(),
    status: types.string(),
    applicant_count: z._default(types.number(), 0),
    interview_count: z._default(types.number(), 0),
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_id": "displayId",
      "applicant_count": "applicantCount",
      "interview_count": "interviewCount",
    });
  }),
);

export function positionJobDataFromJSON(
  jsonString: string,
): SafeParseResult<PositionJobData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PositionJobData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PositionJobData' from JSON`,
  );
}
