/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AssociationEdge,
  AssociationEdge$inboundSchema,
} from "./association-edge.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import { RecordRef, RecordRef$inboundSchema } from "./record-ref.js";

export type RecordAssociationListData = {
  recordRef: RecordRef;
  items?: Array<AssociationEdge> | undefined;
  page: number;
  pageSize: number;
  total: number;
  meta?: { [k: string]: any } | undefined;
};

/** @internal */
export const RecordAssociationListData$inboundSchema: z.ZodMiniType<
  RecordAssociationListData,
  unknown
> = z.pipe(
  z.object({
    record_ref: RecordRef$inboundSchema,
    items: types.optional(z.array(AssociationEdge$inboundSchema)),
    page: z._default(types.number(), 1),
    page_size: z._default(types.number(), 50),
    total: z._default(types.number(), 0),
    meta: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "record_ref": "recordRef",
      "page_size": "pageSize",
    });
  }),
);

export function recordAssociationListDataFromJSON(
  jsonString: string,
): SafeParseResult<RecordAssociationListData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecordAssociationListData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecordAssociationListData' from JSON`,
  );
}
