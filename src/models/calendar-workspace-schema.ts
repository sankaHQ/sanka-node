/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type CalendarWorkspaceSchema = {
  id: string;
  shortId?: string | null | undefined;
  name: string;
  timezone?: string | null | undefined;
};

/** @internal */
export const CalendarWorkspaceSchema$inboundSchema: z.ZodMiniType<
  CalendarWorkspaceSchema,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    short_id: z.optional(z.nullable(types.string())),
    name: types.string(),
    timezone: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "short_id": "shortId",
    });
  }),
);

export function calendarWorkspaceSchemaFromJSON(
  jsonString: string,
): SafeParseResult<CalendarWorkspaceSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CalendarWorkspaceSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CalendarWorkspaceSchema' from JSON`,
  );
}
