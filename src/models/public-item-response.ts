/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type PublicItemResponse = {
  ok: boolean;
  status: string;
  externalId: string;
  itemId?: string | null | undefined;
  ctxId?: string | null | undefined;
};

/** @internal */
export const PublicItemResponse$inboundSchema: z.ZodMiniType<
  PublicItemResponse,
  unknown
> = z.pipe(
  z.object({
    ok: types.boolean(),
    status: types.string(),
    external_id: types.string(),
    item_id: z.optional(z.nullable(types.string())),
    ctx_id: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "external_id": "externalId",
      "item_id": "itemId",
      "ctx_id": "ctxId",
    });
  }),
);

export function publicItemResponseFromJSON(
  jsonString: string,
): SafeParseResult<PublicItemResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicItemResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicItemResponse' from JSON`,
  );
}
