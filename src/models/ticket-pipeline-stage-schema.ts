/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type TicketPipelineStageSchema = {
  id?: string | null | undefined;
  name: string;
  internalValue: string;
  score: number;
  isTerminal: boolean;
  order: number;
};

/** @internal */
export const TicketPipelineStageSchema$inboundSchema: z.ZodMiniType<
  TicketPipelineStageSchema,
  unknown
> = z.pipe(
  z.object({
    id: z.optional(z.nullable(types.string())),
    name: types.string(),
    internal_value: types.string(),
    score: z._default(types.number(), 0),
    is_terminal: z._default(types.boolean(), false),
    order: z._default(types.number(), 0),
  }),
  z.transform((v) => {
    return remap$(v, {
      "internal_value": "internalValue",
      "is_terminal": "isTerminal",
    });
  }),
);

export function ticketPipelineStageSchemaFromJSON(
  jsonString: string,
): SafeParseResult<TicketPipelineStageSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TicketPipelineStageSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TicketPipelineStageSchema' from JSON`,
  );
}
