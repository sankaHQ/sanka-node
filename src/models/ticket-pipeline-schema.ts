/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  TicketPipelineStageSchema,
  TicketPipelineStageSchema$inboundSchema,
} from "./ticket-pipeline-stage-schema.js";

export type TicketPipelineSchema = {
  id?: string | null | undefined;
  name: string;
  internalName: string;
  isDefault: boolean;
  order: number;
  stages?: Array<TicketPipelineStageSchema> | undefined;
};

/** @internal */
export const TicketPipelineSchema$inboundSchema: z.ZodMiniType<
  TicketPipelineSchema,
  unknown
> = z.pipe(
  z.object({
    id: z.optional(z.nullable(types.string())),
    name: types.string(),
    internal_name: types.string(),
    is_default: z._default(types.boolean(), false),
    order: z._default(types.number(), 0),
    stages: types.optional(z.array(TicketPipelineStageSchema$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "internal_name": "internalName",
      "is_default": "isDefault",
    });
  }),
);

export function ticketPipelineSchemaFromJSON(
  jsonString: string,
): SafeParseResult<TicketPipelineSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TicketPipelineSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TicketPipelineSchema' from JSON`,
  );
}
