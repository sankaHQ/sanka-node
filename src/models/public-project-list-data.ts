/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  PublicProject,
  PublicProject$inboundSchema,
} from "./public-project.js";

export type PublicProjectListData = {
  items: Array<PublicProject>;
  page: number;
  pageSize: number;
  total: number;
  nextCursor?: string | null | undefined;
  meta?: { [k: string]: any } | undefined;
};

/** @internal */
export const PublicProjectListData$inboundSchema: z.ZodMiniType<
  PublicProjectListData,
  unknown
> = z.pipe(
  z.object({
    items: z.array(PublicProject$inboundSchema),
    page: types.number(),
    page_size: types.number(),
    total: types.number(),
    next_cursor: z.optional(z.nullable(types.string())),
    meta: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "page_size": "pageSize",
      "next_cursor": "nextCursor",
    });
  }),
);

export function publicProjectListDataFromJSON(
  jsonString: string,
): SafeParseResult<PublicProjectListData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicProjectListData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicProjectListData' from JSON`,
  );
}
