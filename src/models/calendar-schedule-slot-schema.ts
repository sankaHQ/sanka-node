/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type CalendarScheduleSlotSchema = {
  start: string;
  end: string;
};

/** @internal */
export const CalendarScheduleSlotSchema$inboundSchema: z.ZodMiniType<
  CalendarScheduleSlotSchema,
  unknown
> = z.object({
  start: types.string(),
  end: types.string(),
});

export function calendarScheduleSlotSchemaFromJSON(
  jsonString: string,
): SafeParseResult<CalendarScheduleSlotSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CalendarScheduleSlotSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CalendarScheduleSlotSchema' from JSON`,
  );
}
