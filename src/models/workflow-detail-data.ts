/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  WorkflowListItem,
  WorkflowListItem$inboundSchema,
} from "./workflow-list-item.js";

export type WorkflowDetailData = {
  workflow: WorkflowListItem;
  permission: string;
  classifications?: { [k: string]: string } | undefined;
  message: string;
};

/** @internal */
export const WorkflowDetailData$inboundSchema: z.ZodMiniType<
  WorkflowDetailData,
  unknown
> = z.object({
  workflow: WorkflowListItem$inboundSchema,
  permission: z._default(types.string(), "read"),
  classifications: types.optional(z.record(z.string(), types.string())),
  message: z._default(types.string(), "OK"),
});

export function workflowDetailDataFromJSON(
  jsonString: string,
): SafeParseResult<WorkflowDetailData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkflowDetailData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkflowDetailData' from JSON`,
  );
}
