/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  ProspectCompaniesFilters,
  ProspectCompaniesFilters$inboundSchema,
} from "./prospect-companies-filters.js";
import {
  ProspectCompanyData,
  ProspectCompanyData$inboundSchema,
} from "./prospect-company-data.js";

export type ProspectCompaniesData = {
  query?: string | null | undefined;
  parsedFilters: ProspectCompaniesFilters;
  results?: Array<ProspectCompanyData> | undefined;
  count: number;
  providerMeta?: { [k: string]: any } | undefined;
};

/** @internal */
export const ProspectCompaniesData$inboundSchema: z.ZodMiniType<
  ProspectCompaniesData,
  unknown
> = z.pipe(
  z.object({
    query: z.optional(z.nullable(types.string())),
    parsed_filters: ProspectCompaniesFilters$inboundSchema,
    results: types.optional(z.array(ProspectCompanyData$inboundSchema)),
    count: types.number(),
    provider_meta: types.optional(z.record(z.string(), z.any())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "parsed_filters": "parsedFilters",
      "provider_meta": "providerMeta",
    });
  }),
);

export function prospectCompaniesDataFromJSON(
  jsonString: string,
): SafeParseResult<ProspectCompaniesData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProspectCompaniesData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProspectCompaniesData' from JSON`,
  );
}
