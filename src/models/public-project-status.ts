/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type PublicProjectStatus = {
  id?: string | null | undefined;
  name: string;
  internalValue: string;
  order: number;
};

/** @internal */
export const PublicProjectStatus$inboundSchema: z.ZodMiniType<
  PublicProjectStatus,
  unknown
> = z.pipe(
  z.object({
    id: z.optional(z.nullable(types.string())),
    name: types.string(),
    internal_value: types.string(),
    order: z._default(types.number(), 0),
  }),
  z.transform((v) => {
    return remap$(v, {
      "internal_value": "internalValue",
    });
  }),
);

export function publicProjectStatusFromJSON(
  jsonString: string,
): SafeParseResult<PublicProjectStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PublicProjectStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PublicProjectStatus' from JSON`,
  );
}
