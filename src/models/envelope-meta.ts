/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import {
  PaginationMeta,
  PaginationMeta$inboundSchema,
} from "./pagination-meta.js";
import { ToastMessage, ToastMessage$inboundSchema } from "./toast-message.js";

export type EnvelopeMeta = {
  ctxId: string;
  pagination?: PaginationMeta | undefined;
  toast?: ToastMessage | undefined;
};

/** @internal */
export const EnvelopeMeta$inboundSchema: z.ZodMiniType<EnvelopeMeta, unknown> =
  z.pipe(
    z.object({
      ctx_id: types.string(),
      pagination: types.optional(PaginationMeta$inboundSchema),
      toast: types.optional(ToastMessage$inboundSchema),
    }),
    z.transform((v) => {
      return remap$(v, {
        "ctx_id": "ctxId",
      });
    }),
  );

export function envelopeMetaFromJSON(
  jsonString: string,
): SafeParseResult<EnvelopeMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EnvelopeMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EnvelopeMeta' from JSON`,
  );
}
