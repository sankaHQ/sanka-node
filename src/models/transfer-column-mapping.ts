/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type TransferColumnMapping = {
  sourceHeader: string;
  targetField: string;
};

/** @internal */
export type TransferColumnMapping$Outbound = {
  source_header: string;
  target_field: string;
};

/** @internal */
export const TransferColumnMapping$outboundSchema: z.ZodMiniType<
  TransferColumnMapping$Outbound,
  TransferColumnMapping
> = z.pipe(
  z.object({
    sourceHeader: z.string(),
    targetField: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      sourceHeader: "source_header",
      targetField: "target_field",
    });
  }),
);

export function transferColumnMappingToJSON(
  transferColumnMapping: TransferColumnMapping,
): string {
  return JSON.stringify(
    TransferColumnMapping$outboundSchema.parse(transferColumnMapping),
  );
}
