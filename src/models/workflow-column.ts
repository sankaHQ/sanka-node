/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type WorkflowColumn = {
  key: string;
  label: string;
};

/** @internal */
export const WorkflowColumn$inboundSchema: z.ZodMiniType<
  WorkflowColumn,
  unknown
> = z.object({
  key: types.string(),
  label: types.string(),
});

export function workflowColumnFromJSON(
  jsonString: string,
): SafeParseResult<WorkflowColumn, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkflowColumn$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkflowColumn' from JSON`,
  );
}
