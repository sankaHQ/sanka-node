/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CalendarScheduleSlotSchema,
  CalendarScheduleSlotSchema$inboundSchema,
} from "./calendar-schedule-slot-schema.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type CalendarDayScheduleSchema = {
  dayIndex: number;
  dayName: string;
  enabled: boolean;
  slots?: Array<CalendarScheduleSlotSchema> | undefined;
};

/** @internal */
export const CalendarDayScheduleSchema$inboundSchema: z.ZodMiniType<
  CalendarDayScheduleSchema,
  unknown
> = z.pipe(
  z.object({
    day_index: types.number(),
    day_name: types.string(),
    enabled: z._default(types.boolean(), false),
    slots: types.optional(z.array(CalendarScheduleSlotSchema$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "day_index": "dayIndex",
      "day_name": "dayName",
    });
  }),
);

export function calendarDayScheduleSchemaFromJSON(
  jsonString: string,
): SafeParseResult<CalendarDayScheduleSchema, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CalendarDayScheduleSchema$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CalendarDayScheduleSchema' from JSON`,
  );
}
