/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type IncentiveAllocationLine = {
  ownerId: string;
  allocationPercent: number;
};

/** @internal */
export type IncentiveAllocationLine$Outbound = {
  owner_id: string;
  allocation_percent: number;
};

/** @internal */
export const IncentiveAllocationLine$outboundSchema: z.ZodMiniType<
  IncentiveAllocationLine$Outbound,
  IncentiveAllocationLine
> = z.pipe(
  z.object({
    ownerId: z.string(),
    allocationPercent: z.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      ownerId: "owner_id",
      allocationPercent: "allocation_percent",
    });
  }),
);

export function incentiveAllocationLineToJSON(
  incentiveAllocationLine: IncentiveAllocationLine,
): string {
  return JSON.stringify(
    IncentiveAllocationLine$outboundSchema.parse(incentiveAllocationLine),
  );
}
