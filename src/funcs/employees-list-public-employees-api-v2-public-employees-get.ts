/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { SankaCore } from "../core.js";
import { encodeFormQuery, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SankaError } from "../models/errors/sanka-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List Public Employees
 */
export function employeesListPublicEmployeesApiV2PublicEmployeesGet(
  client: SankaCore,
  request?:
    | operations.ListPublicEmployeesApiV2PublicEmployeesGetRequest
    | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.ListPublicEmployeesApiV2PublicEmployeesGetResponse,
    | errors.ErrorEnvelope
    | SankaError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: SankaCore,
  request?:
    | operations.ListPublicEmployeesApiV2PublicEmployeesGetRequest
    | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.ListPublicEmployeesApiV2PublicEmployeesGetResponse,
      | errors.ErrorEnvelope
      | SankaError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      z.parse(
        z.optional(
          operations
            .ListPublicEmployeesApiV2PublicEmployeesGetRequest$outboundSchema,
        ),
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const path = pathToFunc("/v2/public/employees")();

  const query = encodeFormQuery({
    "created_at_from": payload?.created_at_from,
    "created_at_to": payload?.created_at_to,
    "cursor": payload?.cursor,
    "filters": payload?.filters,
    "language": payload?.language,
    "limit": payload?.limit,
    "page": payload?.page,
    "search": payload?.search,
    "sort": payload?.sort,
    "status": payload?.status,
    "updated_at_from": payload?.updated_at_from,
    "updated_at_to": payload?.updated_at_to,
    "usage_status": payload?.usage_status,
    "view_id": payload?.view_id,
    "workspace_id": payload?.workspace_id,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
    "Accept-Language": encodeSimple(
      "Accept-Language",
      payload?.["Accept-Language"],
      { explode: false, charEncoding: "none" },
    ),
    "X-Language": encodeSimple("X-Language", payload?.["X-Language"], {
      explode: false,
      charEncoding: "none",
    }),
    "X-Workspace-Code": encodeSimple(
      "X-Workspace-Code",
      payload?.["X-Workspace-Code"],
      { explode: false, charEncoding: "none" },
    ),
  }));

  const secConfig = await extractSecurity(client._options.bearerAuth);
  const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "list_public_employees_api_v2_public_employees_get",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.bearerAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.ListPublicEmployeesApiV2PublicEmployeesGetResponse,
    | errors.ErrorEnvelope
    | SankaError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(
      200,
      operations
        .ListPublicEmployeesApiV2PublicEmployeesGetResponse$inboundSchema,
      { hdrs: true, key: "Result" },
    ),
    M.jsonErr([401, 422], errors.ErrorEnvelope$inboundSchema, { hdrs: true }),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
